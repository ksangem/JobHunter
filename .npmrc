# Expo + React Native need a hoisted node_modules layout to resolve correctly
# under pnpm. (Metro does not understand pnpm's symlinked store by default.)
node-linker=hoisted
shamefully-hoist=true
strict-peer-dependencies=false
