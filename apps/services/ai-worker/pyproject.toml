[project]
name = "ai-worker"
version = "2.0.0"
description = "JobHunter ai-worker — LLM/embedding worker: CV parsing, ATS scoring, redesign, embeddings (FR-A-005)"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "sqlalchemy>=2.0.36",
    "asyncpg>=0.30.0",
    "pydantic>=2.9.0",
    "pydantic-settings>=2.6.0",
    "anthropic>=0.40.0",
]

[tool.uvicorn]
host = "0.0.0.0"
port = 3011
